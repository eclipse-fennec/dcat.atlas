# Development Guide

> A running, step-by-step log of the development decisions and procedures for the
> Fennec DCAT.Atlas open-data portal. Each entry records **what** we did, **why**,
> and **how to reproduce it**, so the next contributor can follow the same path.
>
> This is an internal document (not published to the docs site). Newest entries
> go at the top of the [Change log](#change-log).

## Repository layout

| Path | Purpose |
|---|---|
| `org.eclipse.fennec.dcat.atlas.dcatap.de.model/` | EMF model bundle for the DCAT-AP.de vocabulary (ecores + generated code + tests). |
| `docs/` | Source-of-truth documentation. Internal docs stay here; the user guide is published via `docs-site/`. |
| `docs-site/` | VitePress documentation site (publishes the curated user guide). |
| `references/` | Specs and reference code (git-ignored, not committed). |

## Working on the EMF model

The model bundle is a **bnd workspace** project. EMF code is generated by bnd via
the `-generate` instruction in `bnd.bnd` (`generate=fennecEMF`, driven by
`model/dcatap.genmodel`). The **ecores are the source of truth**; `src/` is
generated output.

Standard loop for a model change:

1. **Edit the ecore(s)** under `…dcatap.de.model/model/` (e.g. `dcatap.ecore`).
   Each foreign vocabulary lives in its own ecore (`foaf.ecore`, `skos.ecore`,
   `terms.ecore`, `vcard.ecore`, `spdx.ecore`, …). Foreign-namespace *properties*
   are inlined into the `dcat` package with a per-feature `namespace`
   ExtendedMetaData annotation; foreign-namespace *classes* get their own ecore.
2. **Reload the genmodel** from the ecores so new classifiers/features are picked
   up (in `dcatap.genmodel`: right-click → *Reload…*). When you add a **new ecore
   package**, include it in the reload selection so a `genPackage` is created for it.
3. **Regenerate** the code (bnd runs code generation on build; or trigger it from
   the IDE / `./gradlew`).
4. **Run the tests**: `./gradlew :org.eclipse.fennec.dcat.atlas.dcatap.de.model:test`
   (add `--offline` once dependencies are cached).

> ⚠️ Keep hand edits in the ecore consistent with the existing XSD-derived style
> (this model was generated from an XSD): `ExtendedMetaData` annotations,
> containment references, `DocumentRoot`/`DCATAPRoot` document roots, and
> `rdf.ecore#//Resource` / `PlainLiteral` / `DateOrDateTimeLiteral` ranges.

---

## Change log

### 2026-07-07 — Upgrade the DCAT-AP.de model from 2.0.0 to 3.0

**Goal.** Bring the EMF model into line with the
[DCAT-AP.de 3.0 specification](https://www.dcat-ap.de/def/dcatde/3.0/spec/)
(12 Dec 2024). The starting point was DCAT-AP.de **2.0.0** (declared in the
`dcatap.ecore` package documentation).

**How we scoped it.** The authoritative delta is the spec's **§9.1 "Änderungen in
der Version 3.0"** changelog. We extracted §9.1 plus the §4 class tables from
`references/specs/specification.pdf` and diffed them against the current ecores.
Many properties that §9.1 deprecates/removes (`dcatde:politicalGeocodingURI`,
`dcat:granularity`, `owl:versionInfo`, `dcterms:hasVersion/isVersionOf`,
`dcatde:plannedAvailability`, …) simply **did not exist** in this XSD-derived
model, so there was nothing to delete for them.

**Ecore changes.** (all under `…dcatap.de.model/model/`)

- `dcatap.ecore`
  - **New class `DatasetSeries`** (extends `DcatResource`; adds `accrualPeriodicity`,
    `spatial`, `temporal`), registered as a top-level element in `DCATAPRoot` — §4.5.
  - **`Dataset.inSeries`** → `DatasetSeries` — §4.3.
  - **`DcatResource`** (shared base of Catalog/Dataset/DataService/DatasetSeries):
    `publisher` made **mandatory** (`1..1`); added **`applicableLegislation`**.
  - **`Dataset`**: added `dcat:version`, `dcat:hasVersion`, `geodcatap:originator`,
    `geodcatap:custodian`; `spatialResolutionInMeters` & `temporalResolution`
    cardinality `*` → `0..1`.
  - **`Distribution`**: added `applicableLegislation`, `dcatap:availability`,
    `adms:status`, `spdx:checksum`; `byteSize` range `decimal` →
    `nonNegativeInteger` (and `1..*` → `0..1`); resolution fields `1..*` → `0..1`.
  - **`DataService`**: added `dcterms:format`.
  - **`CatalogRecord.conformsTo`**: `0..1` → `0..n`.
  - Package documentation bumped **2.0.0 → DCAT-AP.de 3.0**.
- `terms.ecore` — `PeriodOfTime` `startDate`/`endDate` re-namespaced `schema:` →
  `dcat:` (§9.1.2 / §9.1.14).
- `vcard.ecore` — contact cardinalities harmonized: `hasEmail` `1..1`→`0..n`,
  `hasURL`/`hasTelephone` → `0..n` (§4.10). The existing `OrganizationType`
  already carried all four `vcard:Kind` properties, so no new class was needed.
- `rdf.ecore` — extended the `DateOrDateTime` union's `memberTypes` with `gYear`
  and `gYearMonth`; since every `issued`/`modified`/`startDate`/`endDate` uses
  `DateOrDateTimeLiteral`, this covers the §9.1 date-range errata everywhere at once.
- **New `spdx.ecore`** — foreign-vocab package (`nsURI=http://spdx.org/rdf/terms#`)
  mirroring `adms.ecore`, containing the **`Checksum`** class (`algorithm` `1..1`,
  `checksumValue` `1..1` `xsd:hexBinary`) — §4.12.

**Regeneration note.** `spdx.ecore` is a brand-new package, so the genmodel reload
had to **include `spdx.ecore`** in the package selection (a plain reload only picks
up new classes/features in *existing* packages). After reloading, the code was
regenerated and the `spdx` package appeared under `src/`.

**Deliberately skipped.** `dct:` → `dcterms:` prefix change (the ecores already use
the full `http://purl.org/dc/terms/` URI; the prefix is a serialization concern).

**Tests** (`…/test/de/jena/mdo/dcatap/de/model/ExampleTest.java`).

- Reviewed the existing RDF-loading cases against the regenerated API. One broke:
  `testLoadResource03` asserted `getHasTelephone()` as a single `String`, but
  `vcard:hasTelephone` is now `0..n` (`EList<String>`) — fixed to assert
  `.get(0)`. The other load cases stayed valid.
- Registered the new `SpdxPackage` in the test's `@BeforeEach` setup.
- Added v3 cases:
  - `testDatasetSeriesAndInSeries` — new `DatasetSeries` + `Dataset.inSeries`.
  - `testDatasetVersioningAndRoles` — `dcat:version`, `hasVersion`,
    `geodcatap:originator`/`custodian`.
  - `testDistributionV3Features` — `spdx:checksum`, `availability`, `status`,
    `applicableLegislation`, integer `byteSize`.
  - `testDataServiceFormat` — new `dcterms:format`.
  - `testApplicableLegislationInheritedByAllResources` — inheritance across
    Catalog/Dataset/DataService/DatasetSeries.
  - `testV3RoundTrip` — builds a v3 model, serializes to RDF/XML, parses it back
    and asserts the new elements (`version`, `inSeries` + `DatasetSeries`,
    `spdx:checksum`, `byteSize`) survive the round trip.

**How to verify.**

```bash
./gradlew :org.eclipse.fennec.dcat.atlas.dcatap.de.model:test --offline
```
